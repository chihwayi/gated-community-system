from app.db.session import Base
from app.models.all_models import User, Property, Visitor, Bill, Payment, Notice, Incident, Ticket, Amenity, Booking, Staff, StaffAttendance
# Import all models here so Alembic can find them
